/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        // Creates clean utility tags (e.g., bg-dynamic, text-dynamic, border-dynamic)
        dynamic: {
          DEFAULT: "var(--dynamic-bg)",
          text: "var(--dynamic-text)",
        },
      },
      fontFamily: {
        sans: [
          "system-ui",
          "-apple-system",
          "BlinkMacSystemFont",
          "Segoe UI",
          "sans-serif",
        ],
      },
    },
  },
  plugins: [],
};
